(function (_, $) {
  /*const $passport = $('.passport');
  const $idCard = $('.id-card');

  const typePassportMethods = {
    makeRoute: function ({ controller = 'installment_product', action = 'index' }) {
      return window.location.href = `http://paymart.uz/index.php?dispatch=${controller}.${action}`;
    },
  };

  $passport.on('click', function () {
    typePassportMethods.makeRoute({ action: 'upload-passport' });
  });

  $idCard.on('click', function () {
    typePassportMethods.makeRoute({ action: 'upload-passport-id' });
  });*/
})(Tygh, Tygh.$);